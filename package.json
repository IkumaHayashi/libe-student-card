{
  "name": "libe-student-card",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "run-p dev:*",
    "dev:front": "vite",
    "dev:functions-build": "yarn --cwd functions build:watch",
    "dev:functions-serve": "yarn --cwd functions serve",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "yarn build && firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^9.9.2",
    "konva": "^8.3.11",
    "vue": "^3.2.37",
    "vue-konva": "^3.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.3",
    "typescript": "^4.6.4",
    "vite": "^3.0.7",
    "vue-tsc": "^0.39.5",
    "yarn-run-all": "^3.1.1"
  }
}